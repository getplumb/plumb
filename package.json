{
  "name": "plumb",
  "private": true,
  "version": "0.0.0",
  "description": "Cross-session AI memory \u2014 OSS core",
  "scripts": {
    "build": "turbo run build",
    "build:ci": "turbo run build --filter=@getplumb/core --filter=@getplumb/wiki --filter=@getplumb/wiki-search-service --filter=@getplumb/wiki-worker --filter=@getplumb/plumb",
    "test": "turbo run test",
    "test:ci": "turbo run test --filter=@getplumb/core --filter=@getplumb/wiki --filter=@getplumb/wiki-search-service --filter=@getplumb/wiki-worker --filter=@getplumb/plumb --concurrency=1",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "@modelcontextprotocol/sdk": "^1.27.1",
    "tsx": "^4.21.0",
    "turbo": "^2.0.0"
  },
  "packageManager": "pnpm@10.30.3",
  "engines": {
    "node": ">=22.16.0",
    "pnpm": ">=9.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "esbuild",
      "protobufjs",
      "sharp"
    ]
  }
}
