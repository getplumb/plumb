{
  "name": "@plumb/cli",
  "version": "0.1.0",
  "description": "Plumb CLI — plumb status / connect / export",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "plumb": "./dist/index.js"
  },
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc",
    "test": "echo \"No tests yet\" && exit 0",
    "lint": "echo \"No lint yet\" && exit 0",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@plumb/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
